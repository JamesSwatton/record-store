<div class="title">
    <div class="title-add">
        <h2><a href="/artists">ARTISTS</a></h2>
    </div>

  <p><em>(<%= @artists.count %>)</em></p>

  <div class="title-add">
      <h3>| <a href="/artists/new">Add</a> |</h3>
  </div>
</div>

<div class="search">
  <% @first_char_from_names.each do |char| %>
  <h2><a href="/artists/filter/<%= char.downcase %>"><%= char %></a></h2>
  <% end %>
</div>


<!-- adds tag showing artist was recently added if popped from list %> -->
<!-- index artists alphabetically while ignoring 'The' prefix -->


<div class="index-table">
  <table>
    <body>
      <% @artists.each do |artist| %>
      <% name = artist.swap_the()%>
        <% if name != @recently_added_artist.swap_the() %>
          <tr>
            <td><a href="/artists/<%= artist.id %>"><%= name  %> </a></td>
          </tr>
        <% else %>
          <tr>
            <td><a href="/artists/<%= artist.id %>"><%= name  %> </a>-- recently added --</td>
          </tr>
        <% end %>
      <% end %>
    </body>
  </table>
</div>



<br>
<hr>
<br>

